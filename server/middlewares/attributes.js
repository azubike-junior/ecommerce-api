import isEmpty from 'lodash.isempty'
import ResponseTool from '../utils/response';
import AttributeValueTool from '../tools/attribute_value';

export const findAttribute = (req, res, next) => {
    const {
        attributes
    } = req.body;


}